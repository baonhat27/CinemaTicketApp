import { View, Text, TouchableOpacity } from 'react-native'
import React from 'react'
import styles from './Schedule.scss'

export default function Schedule() {
  return (
    <TouchableOpacity classname={styles.schedule}>
          
    </TouchableOpacity>
  )
}